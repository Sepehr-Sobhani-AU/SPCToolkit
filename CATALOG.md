# SPCToolkit Catalog

_Generated by `tools/generate_catalog.py`. Re-run any time to refresh._

**146 units total** (68 class, 1 module, 77 plugin). **0 missing docstrings** — these are the priority units to document.

Conventions:
- `(plugin)` — class inherits Plugin / AnalysisPlugin / ActionPlugin (the get_name() string follows the arrow)
- `(class)` — other class
- `(module)` — file with no class definitions (utilities / module-level code)
- **MISSING** — no docstring found anywhere in the file (highest-leverage to fill in first)

---

## `application/`

- `analysis_executor.py` _(class)_ **AnalysisExecutor** — Handles asynchronous analysis execution with threading.
- `application_controller.py` _(class)_ **ApplicationController** — Main entry point for all operations.
- `lod_manager.py` _(class)_ **LODManager** — Stateless LOD computation utility using dynamic point budget.
- `rendering_coordinator.py` _(class)_ **RenderingCoordinator** — Manages visibility state and prepares render data for the viewer.

## `core/entities/`

- `analysis_result.py` _(class)_ **AnalysisResult** — Represents the result of an analysis or task performed on data nodes.
- `cad_object.py` _(module)_ **cad_object** — Backwards-compat shim for the CADObject → VectorFeature rename (2026-05-17).
- `class_reference.py` _(class)_ **ClassReference** — A lightweight reference for filtering points by semantic class.
- `clusters.py` _(class)_ **Clusters** — A class for a DataNode's 'data' attribute containing cluster labels and optional colors.
- `colors.py` _(class)_ **Colors** — A class for a DataNode's 'data' attribute containing RGB colors.
- `data_node.py` _(class)_ **DataNode** — Represents a single unit of data, which can either be raw data or derived data.
- `data_nodes.py` _(class)_ **DataNodes** — Manages a collection of DataNode instances, providing methods for adding, retrieving,
- `dist_to_ground.py` _(class)_ **DistToGround** — A class for a DataNode's 'data' attribute containing vertical distances to ground.
- `eigenvalues.py` _(class)_ **Eigenvalues** — A class for a DataNode's 'data' attribute containing eigenvalues.
- `masks.py` _(class)_ **Masks** — A class for a DataNode's 'data' attribute as a boolean mask array.
- `normals.py` _(class)_ **Normals** — A class for a DataNode's 'data' attribute containing surface normals.
- `point_cloud.py` _(class)_ **PointCloud** — Container for a 3D point cloud and its optional per-point attributes.
- `values.py` _(class)_ **Values** — A class for a DataNode's data attribute as a list of point's attribute values.
- `vector_feature.py` _(class)_ **VectorFeature** — Renderable vector-feature geometry attached to a point-cloud cluster.

## `core/services/`

- `analysis_service.py` _(class)_ **AnalysisService** — Executes analysis plugins synchronously.
- `batch_processor.py` _(class)_ **BatchProcessor** — Processes large point clouds in spatial batches with optional overlap between adjacent cells.
- `cache_service.py` _(class)_ **CacheService** — Centralized cache management for reconstructed PointClouds.
- `eigenvalue_utils.py` _(class)_ **EigenvalueUtils** — Utility class for computing and processing eigenvalues from point clouds.
- `linear_region_grower.py` _(class)_ **LinearRegionGrower** — Grows 1-D linear features (cables, pipes, rails, kerbs, edges) from seed points via the RANSAC line engine; modes: axis trace, linearity-connected, hybrid.
- `ransac.py` — 3 units in this file:
  - _(class)_ **RANSACModel** — Interface that every geometric model must implement for use with RANSAC.
  - _(class)_ **LineModel3D** — 3D line defined by a point and a unit direction vector.
  - _(class)_ **RANSAC** — Model-agnostic RANSAC engine (stateless — all state lives in the model).
- `reconstruction_service.py` _(class)_ **ReconstructionService** — Rebuilds PointCloud from derived nodes by applying transformations.
- `strided_spatial_hash.py` _(class)_ **StridedSpatialHash** — Stride-based 2D spatial hash for efficient block lookups.

## `plugins/`

- `plugin_manager.py` _(class)_ **PluginManager** — Manages the discovery, loading, and access to plugins using folder-based menu structure.

## `plugins/000_File/`

- `010_load_project_plugin.py` _(plugin)_ **LoadProjectPlugin → "load_project"** — Action plugin for loading a saved project.
- `020_save_project_plugin.py` _(plugin)_ **SaveProjectPlugin → "save_project"** — Action plugin for saving the current project.
- `030_save_project_as_plugin.py` _(plugin)_ **SaveProjectAsPlugin → "save_project_as"** — Action plugin for saving the current project to a new location.

## `plugins/000_File/000_Import Point Cloud/`

- `000_e57_plugin.py` _(plugin)_ **ImportE57Plugin → "import_e57"** — Import E57 Point Cloud Plugin
- `010_las_laz_plugin.py` _(plugin)_ **ImportLASPlugin → "import_las"** — Import LAS/LAZ Point Cloud Plugin
- `020_ply_plugin.py` _(plugin)_ **ImportPointCloudPlugin → "import_ply"** — Action plugin for importing point cloud files.
- `030_npy_npz_plugin.py` _(plugin)_ **ImportNPYPlugin → "import_npy_npz"** — Import NPY/NPZ Point Cloud Plugin
- `060_semantickitti_plugin.py` _(plugin)_ **ImportSemanticKITTIPlugin → "import_semantickitti"** — Plugin for importing SemanticKITTI .bin point cloud files.

## `plugins/000_File/040_Export Point Cloud/`

- `000_e57_plugin.py` _(plugin)_ **ExportE57Plugin → "export_e57"** — Export E57 Plugin
- `010_las_laz_plugin.py` _(plugin)_ **ExportLASPlugin → "export_las"** — Export LAS/LAZ Plugin
- `020_ply_plugin.py` _(plugin)_ **ExportPointCloudPlugin → "export_ply"** — Export Point Cloud Plugin

## `plugins/010_View/`

- `000_reset_view_plugin.py` _(plugin)_ **ResetViewPlugin → "reset_view"** — Plugin for resetting the camera to its default position and orientation.
- `005_zoom_to_extent_plugin.py` _(plugin)_ **ZoomToExtentPlugin → "zoom_to_extent"** — Frame the camera so the full visible point cloud fills the viewport.
- `010_zoom_window_plugin.py` _(plugin)_ **ZoomWindowPlugin → "zoom_window"** — Action plugin that activates zoom window mode in the 3D viewer.
- `020_point_size_plugin.py` _(plugin)_ **PointSizePlugin → "point_size"** — Action plugin for adjusting the 3D viewer point size.
- `050_flythrough_plugin.py` — 2 units in this file:
  - _(class)_ **FlythroughDialog** — Non-modal dialog for managing flythroughs and running camera animation.
  - _(plugin)_ **FlythroughPlugin → "flythrough"** — Opens the Flythrough dialog for smooth camera path animation.
- `060_preview_data_plugin.py` _(plugin)_ **PreviewDataPlugin → "preview_data"** — Action plugin for previewing saved data.

## `plugins/015_Branch/`

- `000_delete_branch_plugin.py` _(plugin)_ **DeleteBranchPlugin → "delete_branch"** — Plugin to delete selected branches and all their children from the tree.
- `010_merge_branches_plugin.py` _(plugin)_ **MergeBranchesPlugin → "merge_branches"** — Action plugin for merging multiple selected branches into one PointCloud.
- `020_subtract_plugin.py` _(plugin)_ **SubtractPlugin → "subtract"** — Plugin for subtracting one point cloud from another.
- `030_duplicate_to_root_plugin.py` _(plugin)_ **DuplicateToRootPlugin → "duplicate_to_root"** — Promote a reconstructed branch to an independent root point cloud.

## `plugins/020_Points/000_Subsampling/`

- `000_subsampling_plugin.py` _(plugin)_ **SubsamplingPlugin → "subsampling"** — Plugin for subsampling point cloud data.
- `010_density_subsampling_plugin.py` _(plugin)_ **DensitySubsamplingPlugin → "density_subsampling"** — Plugin for density-based subsampling of point clouds using Open3D.
- `020_voxel_subsample_plugin.py` _(plugin)_ **VoxelSubsamplePlugin → "voxel_subsample"** — GPU-accelerated voxel-grid subsampling.

## `plugins/020_Points/010_Filtering/`

- `000_filtering_plugin.py` _(plugin)_ **FilteringPlugin → "filtering"** — Plugin for filtering point cloud data based on a condition.
- `010_sor_plugin.py` _(plugin)_ **SORPlugin → "sor"** — Statistical Outlier Removal (SOR) plugin for noise filtering.

## `plugins/020_Points/020_Clustering/`

- `000_dbscan_plugin.py` _(plugin)_ **DBSCANPlugin → "dbscan"** — DBSCAN clustering algorithm plugin with integrated batch processing.
- `010_hdbscan_plugin.py` _(plugin)_ **HDBSCANPlugin → "hdbscan"** — HDBSCAN clustering algorithm plugin with integrated batch processing.
- `020_cluster_size_filter_plugin.py` _(plugin)_ **ClusterSizeFilterPlugin → "cluster_size_filter"** — Plugin for filtering clusters based on the number of points they contain.
- `030_surface_region_growing_plugin.py` _(plugin)_ **SurfaceRegionGrowingPlugin → "surface_region_growing"** — Plugin for surface region growing on a pre-existing DBSCAN Clusters branch.
- `040_linear_region_growing_plugin.py` _(plugin)_ **LinearRegionGrowingPlugin → "linear_region_growing"** — Semi-automated linear-feature region growing (cables, pipes, rails, kerbs, edges) from picked seed points; axis-trace / linearity-connected / hybrid modes.

## `plugins/020_Points/030_Analysis/`

- `000_compute_eigenvalues_plugin.py` _(plugin)_ **ComputeEigenvaluesPlugin → "compute_eigenvalues"** — Plugin for computing eigenvalues from local point neighborhoods.
- `010_knn_analysis_plugin.py` _(plugin)_ **KNNAnalysisPlugin → "knn_analysis"** — Plugin for computing K-Nearest Neighbors statistics.
- `020_geometric_classification_plugin.py` _(plugin)_ **GeometricClassificationPlugin → "geometric_classification"** — Classify each point into a geometric shape class based on eigenvalue features.
- `021_planar_classification_plugin.py` _(plugin)_ **PlanarClassificationPlugin → "planar_classification"** — Classify each point as Planar or Non-Planar based on eigenvalue planarity.
- `022_linear_classification_plugin.py` _(plugin)_ **LinearClassificationPlugin → "linear_classification"** — Classify each point as Linear or Non-Linear based on eigenvalue linearity.
- `023_scatter_classification_plugin.py` _(plugin)_ **ScatterClassificationPlugin → "scatter_classification"** — Classify each point as Scatter or Non-Scatter based on eigenvalue sphericity.
- `024_cylindrical_classification_plugin.py` _(plugin)_ **CylindricalClassificationPlugin → "cylindrical_classification"** — Classify each point as Cylindrical or Non-Cylindrical based on eigenvalue linearity and planarity.
- `025_sparse_classification_plugin.py` _(plugin)_ **SparseClassificationPlugin → "sparse_classification"** — Classify each point as Sparse or Non-Sparse based on total variance percentile outliers.
- `027_vegetation_classification_plugin.py` _(plugin)_ **VegetationClassificationPlugin → "vegetation_classification"** — Classify points as Vegetation or Non-Vegetation using multi-scale eigenvalue closeness.
- `028_curvature_edge_filter_plugin.py` _(plugin)_ **CurvatureEdgeFilterPlugin → "curvature_edge_filter"** — Filter a point cloud to keep only points on geometric edges.
- `030_split_geometric_classes_plugin.py` _(plugin)_ **SplitGeometricClassesPlugin → "split_geometric_classes"** — Split a named Clusters node into one ClassReference branch per class.
- `035_normal_estimation_plugin.py` _(plugin)_ **NormalEstimationPlugin → "estimate_normals"** — Plugin for estimating surface normals from local point neighborhoods.
- `040_surface_fit_plugin.py` _(plugin)_ **SurfaceFitPlugin → "surface_fit"** — Surface Fit Plugin
- `045_mesh_drape_plugin.py` _(plugin)_ **MeshDrapePlugin → "mesh_drape"** — Mesh Drape Plugin
- `050_projected_distance_plugin.py` _(plugin)_ **ProjectedDistancePlugin → "projected_distance"** — Projected Distance Plugin

## `plugins/030_Selection/`

- `000_separate_selected_points_plugin.py` _(plugin)_ **SeparateSelectedPointsPlugin → "separate_selected_points"** — Plugin for separating selected points into a new branch.
- `010_separate_selected_clusters_plugin.py` _(plugin)_ **SeparateSelectedClustersPlugin → "separate_selected_clusters"** — Plugin for separating selected clusters into a new branch.

## `plugins/040_Clusters/`

- `000_classify_cluster_plugin.py` _(plugin)_ **ClassifyClusterPlugin → "classify_cluster"** — Action plugin for manually classifying clusters.
- `005_merge_classified_layers_plugin.py` _(plugin)_ **MergeClassifiedLayersPlugin → "merge_classified_layers"** — Plugin for merging multiple branches into a single classified PointCloud.
- `010_export_classified_clusters_plugin.py` _(plugin)_ **ExportClassifiedClustersPlugin → "export_classified_clusters"** — Export classified clusters as numpy arrays for training data preparation.
- `020_split_classes_plugin.py` — 2 units in this file:
  - _(class)_ **ClassSelectionDialog** — Dialog for selecting which classes to split into branches.
  - _(plugin)_ **SplitClassesPlugin → "split_classes"** — Action plugin for splitting Clusters with names into separate class branches.
- `030_cluster_by_class_plugin.py` _(plugin)_ **ClusterByClassPlugin → "cluster_by_class"** — Cluster by Class Action Plugin.
- `040_cluster_by_value_plugin.py` _(plugin)_ **ClusterByValuePlugin → "cluster_by_value"** — Cluster by Value Action Plugin.
- `044_undo_cluster_edit_plugin.py` _(plugin)_ **UndoClusterEditPlugin → "undo_cluster_edit"** — Plugin for undoing the last cluster edit (remove, cut, or merge) on the selected branch.
- `045_lock_unlock_clusters_plugin.py` — 2 units in this file:
  - _(class)_ **LockUnlockDialog** — Plugin for locking/unlocking clusters against remove, cut, and merge operations.
  - _(plugin)_ **LockUnlockClustersPlugin → "lock_unlock_clusters"** — Plugin for locking/unlocking clusters against remove, cut, and merge operations.
- `046_color_clusters_plugin.py` _(plugin)_ **ColorClustersPlugin → "color_clusters"** — Plugin for assigning a custom color to selected clusters.
- `050_cut_cluster_plugin.py` _(plugin)_ **CutClusterPlugin → "cut_cluster"** — Plugin for cutting (splitting) selected points out of their cluster(s) into new clusters.
- `060_merge_clusters_plugin.py` _(plugin)_ **MergeClustersPlugin → "merge_clusters"** — Plugin for merging multiple clusters into one.
- `070_remove_clusters_plugin.py` _(plugin)_ **RemoveClustersPlugin → "remove_clusters"** — Plugin for removing (deleting) selected clusters by setting them to noise (-1).

## `plugins/050_Processing/`

- `010_average_distance_plugin.py` _(plugin)_ **AverageDistancePlugin → "average_distance"** — Plugin to calculate the average distance of each point to its k nearest neighbors.

## `plugins/060_Infrastructure/`

- `000_power_line_detection_plugin.py` _(plugin)_ **PowerLineDetectionPlugin → "power_line_detection"** — Semi-automated power line (cable) detection plugin.
- `020_generate_vector_features_plugin.py` — 2 units in this file:
  - _(class)_ **FeatureClassDialog** — Let the user choose geometry type per class.
  - _(plugin)_ **GenerateVectorFeaturesPlugin → "generate_vector_features"** — Generate vector-feature branches for classified clusters.
- `030_cluster_boundary_plugin.py` _(plugin)_ **ClusterBoundaryPlugin → "cluster_boundary"** — Create a closed 3D polyline boundary for each cluster.

## `plugins/070_ML_Models/000_PointNet/000_Classification/`

- `000_generate_training_data_plugin.py` _(plugin)_ **GenerateTrainingDataPlugin → "generate_training_data"** — Generate training data for PointNet feature classification.
- `005_analyze_classes_plugin.py` _(plugin)_ **AnalyzeClassesPlugin → "analyze_classes"** — Action plugin for analyzing and preparing imbalanced class data.
- `010_train_model_plugin.py` — 2 units in this file:
  - _(class)_ **PointCloudDataset** — PyTorch Dataset for variable-size point cloud data with random sampling.
  - _(plugin)_ **TrainPointNetPlugin → "train_pointnet_model"** — Action plugin for training PointNet model for cluster classification.
- `020_classify_clusters_plugin.py` _(plugin)_ **ClassifyClustersMLPlugin → "classify_clusters"** — Action plugin for ML-based cluster classification using PointNet (PyTorch).

## `plugins/070_ML_Models/000_PointNet/010_Segmentation/`

- `000_generate_seg_training_data_plugin.py` _(plugin)_ **GenerateSegTrainingDataPlugin → "generate_seg_training_data"** — Generate training data for PointNet segmentation from classified clusters.
- `005_import_external_dataset_plugin.py` _(plugin)_ **ImportExternalDatasetPlugin → "import_external_dataset"** — Import external labeled datasets for segmentation training.
- `010_annotate_points_plugin.py` _(plugin)_ **AnnotatePointsPlugin → "annotate_points"** — Action plugin to open manual annotation mode.
- `020_train_seg_model_plugin.py` — 5 units in this file:
  - _(class)_ **FocalLoss** — Focal loss for addressing class imbalance in segmentation.
  - _(class)_ **DiceLoss** — Dice loss for segmentation — optimizes region overlap directly.
  - _(class)_ **CombinedFocalDiceLoss** — Combined Focal + Dice loss — balances pixel-level and region-level learning.
  - _(class)_ **SegPointCloudDataset** — PyTorch Dataset for segmentation training data (.npz files with features + labels).
  - _(plugin)_ **TrainSegModelPlugin → "train_seg_model"** — Action plugin for training PointNet segmentation model.
- `030_segment_point_cloud_plugin.py` _(plugin)_ **SegmentPointCloudPlugin → "segment_point_cloud"** — Action plugin for semantic segmentation of point clouds using PointNet.

## `plugins/070_ML_Models/010_PointNet2/010_Segmentation/`

- `000_train_model_plugin.py` _(plugin)_ **TrainPointNet2SegPlugin → "train_pointnet2_seg_model"** — Action plugin for training PointNet++ SSG segmentation model.
- `010_segment_point_cloud_plugin.py` _(plugin)_ **SegmentPointCloudPP → "segment_point_cloud_pp"** — Action plugin for semantic segmentation using a trained PointNet++ SSG model.

## `plugins/085_Tools/`

- `000_dbscan_benchmark_plugin.py` _(plugin)_ **DBSCANBenchmarkPlugin → "dbscan_benchmark"** — Action plugin for benchmarking clustering implementations.

## `plugins/095_Plugins/`

- `000_manage_plugins_plugin.py` — 2 units in this file:
  - _(class)_ **ManagePluginsDialog** — Dialog for managing plugins: view, reload, unload, scan for new.
  - _(plugin)_ **ManagePluginsPlugin → "manage_plugins"** — Open the Manage Plugins dialog (view loaded plugins, reload, unload, scan).

## `plugins/100_Help/`

- `000_system_info_plugin.py` _(plugin)_ **SystemInfoPlugin → "system_info"** — Action plugin for displaying system and hardware information.

## `plugins/backends/`

- `backend_registry.py` _(class)_ **BackendRegistry** — Selects and provides access to the best available backend for each algorithm.
- `base.py` — 7 units in this file:
  - _(class)_ **BaseBackend** — Base class for all backends with common logging functionality.
  - _(class)_ **DBSCANBackend** — Abstract base class for DBSCAN clustering backends.
  - _(class)_ **HDBSCANBackend** — Abstract base class for HDBSCAN clustering backends.
  - _(class)_ **KNNBackend** — Abstract base class for K-Nearest Neighbors backends.
  - _(class)_ **MaskingBackend** — Abstract base class for point cloud masking/filtering backends.
  - _(class)_ **EigenvalueBackend** — Abstract base class for eigenvalue computation backends.
  - _(class)_ **NormalEstimationBackend** — Abstract base class for normal estimation backends.
- `dbscan_backends.py` — 3 units in this file:
  - _(class)_ **CuMLDBSCAN** — GPU-accelerated DBSCAN using NVIDIA cuML (RAPIDS).
  - _(class)_ **SklearnDBSCAN** — CPU DBSCAN using scikit-learn (multi-threaded).
  - _(class)_ **Open3DDBSCAN** — CPU DBSCAN using Open3D.
- `eigenvalue_backends.py` — 2 units in this file:
  - _(class)_ **PyTorchCUDAEigen** — GPU-accelerated eigenvalue computation using PyTorch CUDA.
  - _(class)_ **PyTorchCPUEigen** — CPU eigenvalue computation using PyTorch.
- `hdbscan_backends.py` — 2 units in this file:
  - _(class)_ **CuMLHDBSCAN** — GPU-accelerated HDBSCAN using NVIDIA cuML (RAPIDS).
  - _(class)_ **SklearnHDBSCAN** — CPU HDBSCAN using scikit-learn (multi-threaded).
- `knn_backends.py` — 2 units in this file:
  - _(class)_ **CuMLKNN** — GPU-accelerated KNN using NVIDIA cuML (RAPIDS).
  - _(class)_ **ScipyKNN** — CPU KNN using scipy KDTree.
- `masking_backends.py` — 2 units in this file:
  - _(class)_ **CuPyMasking** — GPU-accelerated masking using CuPy.
  - _(class)_ **NumpyMasking** — CPU masking using NumPy.
- `normal_estimation_backends.py` — 3 units in this file:
  - _(class)_ **PyTorchCUDANormals** — GPU-accelerated normal estimation using PyTorch CUDA.
  - _(class)_ **Open3DCUDANormals** — GPU normal estimation using Open3D CUDA tensor pipeline.
  - _(class)_ **Open3DNormals** — CPU normal estimation using Open3D legacy pipeline.

## `plugins/dialogs/`

- `annotation_window.py` _(class)_ **AnnotationWindow** — Dialog for manual point cloud annotation.
- `class_analysis_window.py` _(class)_ **ClassAnalysisWindow** — Dialog for analyzing and preparing imbalanced class data.
- `class_subsample_dialog.py` _(class)_ **ClassSubsampleDialog** — Dialog with a sortable table listing each class, its point percentage, ratio, and resulting count.
- `classification_progress_dialog.py` _(class)_ **ClassificationProgressDialog** — Progress dialog for cluster classification.
- `data_generation_progress_dialog.py` _(class)_ **DataGenerationProgressDialog** — Dialog to display data generation progress.
- `shift_dialog.py` _(class)_ **ShiftDialog** — Dialog showing the detected coordinate shift, editable by the user.
- `training_data_preview_window.py` _(class)_ **DataPreviewWindow** — Generic dialog window for previewing saved data.
- `training_progress_window.py` _(class)_ **TrainingProgressWindow** — Window to display training progress with GPU monitoring.
