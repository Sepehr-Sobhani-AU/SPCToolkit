@echo off
REM Windows batch file to launch SPCToolkit in GPU mode (WSL2)

echo ============================================================
echo Launching SPCToolkit in GPU Mode (WSL2 + RAPIDS)
echo ============================================================
echo.

wsl bash run_gpu.sh
